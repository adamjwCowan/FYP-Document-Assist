[pytest]
# Add the source directory to PYTHONPATH so tests can import your modules
pythonpath = src

# Look for test files in the tests/ directory
testpaths = tests

# Treat test files matching this pattern
python_files = test_*.py

# Optional: additional command-line options
addopts = -ra -q